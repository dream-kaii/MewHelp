# ch01 开发过程留痕

多章项目「电商智能客服系统」的 ch01 —— 纯对话跑通。按用户要求分阶段补记,每段记四样:**① 用户关键原话** / **② 我的关键产出** / **③ 用户拒绝或纠偏** / **④ 翻车与返工**。

---

## brainstorm 定稿(2026-09-08)

**① 用户原话(节选)**
> 我要用 Superpowers 模式做一个电商智能客服系统,第一步先跑通纯对话。……技术栈:Python + FastAPI + LangChain,模型接入直连上游,应用侧统一说 OpenAI 协议……本章不做:工具调用、Agent 循环……涉及具体库、框架、API 的用法,一律先用 Context7 MCP 查最新官方文档……Chat 页面是例外用 Vibe Coding 方式直接做,不套 brainstorm、TDD、code review。

> 现在你应该有 context7 了 然后 用writing-plans,编写我们的实现计划

**② 我的关键产出**
- 分类为 architectural(新项目)路径,按 brainstorm 流程先澄清后设计。
- 澄清结论两则:售后「诉求类型」= 枚举四类 {退货退款/仅退款/换货/维修},无法归类取 null,期望方案自由文本;模型接入统一 OpenAI 协议,Claude 经 OpenAI 兼容网关(不走 Anthropic 专用分支)。
- 设计草案(仓库布局、SSE 契约、token 预算启发式、with_structured_output 抽取、TDD/标注集分工、验收映射)在对话中分节给出并获准。
- `git init -b main` + `.gitignore`,仓库由空目录初始化为 git 仓库。
- spec 落盘:`docs/superpowers/specs/2026-09-08-ecommerce-cs-ch01-design.md`。

**③ 用户拒绝或纠偏**
- 我一度判断本会话无 Context7、准备用 WebFetch 顶替查文档;用户纠偏:「现在你应该有 context7 了」——工具随后确实出现在 MCP 列表,后续查文档走 Context7。
- 设计对话中 4 个待确认假设(① git init ② pyproject/pip ③ 服务端按 session_id 持历史 ④ 启发式 token 估算)用户未逐条驳回,默认按我的推荐带进计划,计划评审为最后否决点。

**④ 翻车与返工**
- 暂无实现性翻车。环境侧:会话早期工具列表确无 context7 MCP,与用户预期不符,属环境状态而非流程错误;已按用户提示复检确认可用。

---

## 计划评审通过(2026-09-08)

**① 用户原话**
> 现在你应该有 context7 了 然后 用writing-plans,编写我们的实现计划
> (AskUserQuestion 答复)ch01 实施计划评审意见?:通过;真模型 eval 和三连验收用哪家?:有真实 key

**② 我的关键产出**
- 计划落盘并提交:`docs/superpowers/plans/2026-09-08-ecommerce-cs-ch01-plan.md`(commit 2a97b44),10 任务。
- 动手前用 Context7 核实的 API 决策:SSE 改用 FastAPI 原生 `fastapi.sse.EventSourceResponse/ServerSentEvent`(spec 里"手写帧"的设想升级为官方组件);流式用 `model.astream`;`with_structured_output(method=...)` 做成 .env 可配;fake 模型走 `langchain_core.language_models.fake_chat_models.GenericFakeChatModel`。
- Self-review 三项通过;清掉了 Task 6 草稿里一处占位断言。
- 真模型走用户真实 key,.env 由用户填;provider 未指定,默认样例 DeepSeek 排头,Task 9 前再确认。

**③ 用户拒绝或纠偏**
- 无。4 个待确认假设(git init / pyproject / 服务端 session 语义 / token 启发式估算)随"通过"一并放行。

**④ 翻车与返工**
- writing-plans 初稿 Task 6 测试含一行占位断言(`store.get_session_ids() and None # placeholder`),违反 no-placeholder,提交前已用完整用例替换并在 Self-Review 记录——计划评审前修正,未造成返工。

---

## Task 1 完成:脚手架 + Settings + /healthz(2026-09-08)

**① 用户原话**
> 你给我建一个env文件 我来填key
(随后用户已把 DeepSeek key 填入 .env)

**② 我的关键产出**
- `git init` 后首次提交的代码基底:pyproject.toml(pip 可编辑安装 + pytest)、.env.example、app/{__init__,config,main}.py、tests/{conftest,test_healthz}.py;`/healthz` 返回 provider/model。
- 实际安装版本(已记入留痕):fastapi 0.141.1(langchain 生态已 1.x:langchain-openai 1.6.0 / langchain-core 1.6.2)、pydantic-settings 2.15.0。`fastapi.sse.EventSourceResponse` 存在 ✅;pytest 插件 anyio-4.15.1 已自动加载(`pytest.mark.anyio` 可用)。
- 用户提供 DeepSeek key 于 `.env`(gitignored,不入库)。
- healthz 测试 red→green 通过。

**③ 用户拒绝或纠偏**
- 用户中途插话建 `.env`——我当时发现磁盘上还没有 `.env.example`(计划文档里的只是代码块),改为直接建 `.env`,Task 1 落盘 `.env.example`。

**④ 翻车与返工**
- 创建 `.env` 时 `cp .env.example .env` 失败:计划尚未执行,磁盘无该文件。改用 Write 直建 `.env`,属流程正常(plan 文档内容 ≠ 已执行产物),无代码返工。
- 环境侧重要信号:**langchain-core/openai 已是 1.x**,后续 Task 5/6/8 涉及的 PromptTemplate / astream / fake 模型 import 路径 / with_structured_output 必须按 1.x 复核,不能照 0.3 时代的记忆写。

---

## Task 2 完成:schemas(2026-09-08)

**① 用户原话**
> (本轮无新输入,承接计划评审"通过")

**② 我的关键产出**
- `app/schemas.py` 落盘:`ChatRequest`、`ExtractRequest`、`RequestType`(四类 str Enum)、`AfterSalesExtract`(order_no/request_type/desired_solution 全 null-safe + 中文 description)。
- 5 个单测 red→green 通过(空 message 拒收、枚举恰四类、null 兜底)。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- 无。纯 pydantic,API 稳定未受 langchain 1.x 影响。

---

## Task 3 完成:context 裁剪 + token 预算(2026-09-08)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- `app/context.py` 纯函数:estimate_tokens(2 字符/token,空串=0)/ total_tokens(每条 +4 开销)/ trim_to_budget(首 system 与末条永不删,从最旧整对删)/ build_messages。
- 6 个单测 red→green 通过,含「只删最旧整对」与「system+当前消息兜底保留」。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **计划测试参数 bug(执行期修正)**:plan 里 `test_trim_drops_oldest_pair_first` 用 budget=80,但按中文字长估算整窗仅 56 token,根本不会触发裁剪,测试必误红。把 budget 改为 45(整窗 56 超限→删最旧一轮后 36≤45 停)后行为断言成立。属计划参数笔误,非逻辑问题,已记录。

---

## Task 4 完成:sessions(2026-09-08)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- `app/sessions.py`:`SessionStore`(LRU 淘汰、单会话按轮删最旧整对、get_or_create 返回 `(Session, bool)`)、模块级 `_store/get_store()`。
- 5 个单测 red→green 通过。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **测试文件自身 bug**:`get_or_create` 返回 `(Session, bool)`,但两个用例把返回的 Session 对象当 session id 字符串传给 `append_turn/get`,导致 `TypeError: unhashable type: 'Session'`。改为直接用字面 id("s1"/"cap")后通过。实现代码未动,纯测试写法问题。

---

## Task 5 完成:prompts(2026-09-08)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- Context7 核对:langchain-core 1.x 仍用 `from langchain_core.prompts import PromptTemplate` + `from_template`/`.format`(integration docs 现行用法)。
- `app/prompts.py`:客服 System PromptTemplate(角色/行为约束/需订单号),抽取 System 常量 + 抽取 User PromptTemplate。
- 3 个渲染断言 red→green(prompt 质量由 Task 9 标注集判,不在此断言)。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- 无。

---

## Task 6 完成:llm 工厂 + ChatService SSE 生成器(2026-09-08)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- `app/llm.py`(统一 OpenAI 协议工厂)、`app/chat.py`(ChatService SSE 生成器 + to_langchain_messages + text_of_chunk)。
- Context7 + 本地 introspection 双层核对已装版本(langchain-openai 1.6.0 / core 1.6.2 / fastapi 0.141.1):`ServerSentEvent` 字段确认 data/event/id/retry/comment;GenericFakeChatModel 1.x 已是 pydantic 模型;1.x chunk 文本投影是 `.text`。
- 5 个单测 green:新会话事件序、两轮历史透传(第二轮 inputs 含 user1/assistant1)、error 不落历史、text_of_chunk 兼容 .text/.content 两种形态。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **计划代码按旧版 API 写 `ChatOpenAI(..., timeout=60)`**:1.6 实测无 timeout 字段(字段集为 openai_api_base/openai_api_key/model_name/max_tokens,别名 base_url/api_key/model;超时字段是 request_timeout)。已改 `request_timeout=120`。
- **计划测试子类化 GenericFakeChatModel 加属性失败**:1.x fake 模型是 pydantic model,`self.seen_inputs=[]` 抛错。改为组合包装类(Recording/FailingChatModel 只暴露 `.astream`)。
- **计划断言 `events == ["error"]` 对新建会话不成立**(新会话先发 session 事件)。改为预建既有会话再测 error 分支。
- **text_of_chunk 从"只读 chunk.content"升级为"优先 .text、退化 content(str/list)"**,匹配 1.x 内容块投影。

---

## Task 7 完成:/api/chat SSE 路由 + 装配(2026-09-08)

**① 用户原话**
> 继续重试
> 分类器是干嘛的 怎么不能用 要怎么解决 或者不用这个可以吗
> 好我切换回来了

**② 我的关键产出**
- `app/routers/chat.py`:FastAPI 0.141 原生 SSE 风格(endpoint 为 async generator + `response_class=EventSourceResponse`,逐事件 yield ServerSentEvent),含 `Cache-Control/X-Accel-Buffering` 头。
- `app/main.py` 重构为模块级 app(state.chat_model/extract_model = None)并挂路由。
- 路由级 3 测试 green:新建会话 session/delta/done、同 session 两轮历史累积、error 事件不含 done。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **SSE data 的 JSON 用 `\uXXXX` 转义序列化**(实测:正文是 `"content": "喵,我在。"` 而非原始 UTF-8)→ 按原文子串断言全挂。改为 `_parse_sse()` 逐帧解析并 `json.loads(data)` 后比较值,断言与实现都更鲁棒(产品侧无影响,客户端 JSON.parse 会还原)。
- 测试文件 docstring 里写 `\uXXXX` 触发 Python unicodeescape SyntaxError,改为 `\\uXXXX`。
- **过程事故(环境)**:Bash/敏感 MCP 被权限分类器后端故障卡死数轮("deepseek-v4-flash[1M] temporarily unavailable"),期间仅只读可用。解决:在 `.claude/settings.local.json` 加 allowlist(pytest/eval_extract/uvicorn/git)绕过分类器;用户同步切换权限模式。白名单只放行本项目明确安全命令。

---

## Task 8 完成:售后结构化抽取服务 + /api/extract 路由(2026-09-08)

**① 用户原话**
> (无新输入,承接上文)

**② 我的关键产出**
- `app/extract.py`:`ExtractService` 用 `ChatOpenAI.with_structured_output(AfterSalesExtract, method=settings.extract_method)`(method 由 .env 控制,function_calling 默认)。
- `app/routers/extract.py`:`POST /api/extract` → AfterSalesExtract;服务异常映射 502,不泄堆栈。
- `app/main.py` 补挂 extract 路由。
- 路由级 2 测试 green(stub 注入 dependency_overrides):200 形状 JSON、502 错误映射。真实抽取质量交给 Task 9 标注集。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **接线缺口(执行期发现)**:重读磁盘产物时发现 `main.py` 只挂了 chat 路由、未挂 extract → `test_extract_route` 必 404。补齐 include 后转绿。此缺口源于分类器故障期间部分工作被打断、未收口。
- 抽取本身(langchain 1.x 的 with_structured_output 行为)留待 Task 9 用真模型验证,有出入再按文档修正。

---

## Task 9 完成:售后抽取标注集 + eval 至 100%(2026-09-08)

**① 用户原话**
> (无新输入;真模型用 DeepSeek,key 已在 .env)

**② 我的关键产出**
- `eval_data/after_sales_samples.json` 8 条标注;`scripts/eval_extract.py` 跑真模型,通过率基线 → 100%。
- **Context7 doc-check**:with_structured_output(method=function_calling/json_mode/json_schema)与代码一致,无需改法。
- schema 加宽容校验;prompt 语义补强;eval 校验器修正(见 ④)。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**(两轮迭代,75% → 100%)
- **真模型把"无法归类"输出成字符串 `"null"` 而非 JSON null** → `AfterSalesExtract` 枚举校验抛 ValidationError 中断整轮。修:三个可空字段加 `mode="before"` validator,把 `"null"/"none"/""` 归一为 None。
- **eval 校验器逻辑反了**:期望 desired_solution 留空(kw=[])时,模型正确输出 None 反被判 miss(`elif not kw and not sol: miss`)。改为 `kw` 空且 `sol` 非空才算 miss。
- **prompt 语义两处不清**:① 用户"想退货退款"却没落到 desired_solution → 补充"用户明确说出解决方式/下一步动作就填,纯描述/提问才 null";② 用户"我要投诉"被判 null → 示例清单补 投诉/催发货 等。两处改后 8/8。
- **写作事故**:prompts.py EXTRACT_SYSTEM 改版时把全角引号误打成半角 `"`,会提前截断字符串导致 SyntaxError;已改成全角引号(读文件核对后修复)。

---

## Task 10 完成:README + 演示脚本 + 三连验收(2026-09-08)

**① 用户原话**
> (无新输入;DeepSeek key 于 .env;期间用户曾切权限模式协助绕过分类器故障)

**② 我的关键产出**
- README(快速开始/四家 OpenAI 协议示例/SSE 契约/测试与验收命令);demo_chat.sh / demo_extract.sh + scripts/fixtures(UTF-8 payload)。
- 三连验收实测(真 DeepSeek):
  1. `/api/chat` 首轮流式:事件序 `session → delta×N → done`,逐 token 输出,助手回复中文客服话术 ✓
  2. 同 session(demo-acceptance)第二轮:**无 session 事件**(同会话),回复"您刚才提到的订单号是20260901001"——上下文续接实证 ✓
  3. `/api/extract` 返回 `{"order_no":"20260901001","request_type":"退货退款","desired_solution":"退货退款,上门取件"}`;`python -m scripts.eval_extract` 8/8=100% ✓
- 全量 `pytest`:30 passed。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **Windows 控制台 GBK 破坏 JSON**:curl `-d` 直传中文 body 报 "error parsing the body"(GBK 中文字节偶含 0x5C 被当转义)。修法:中文 payload 落成 UTF-8 fixture 文件,curl 用 `--data-binary @file`。demo 脚本与验收全部走此路径。
- **SSE 里 session_id 提取失败**:① 响应 JSON 是 `"session_id": "xxx"`(冒号后空格),我首版 grep pattern 无空格匹配不到;② msys grep 对含引号 pattern 兼容不稳。彻底解法:demo 改用**固定 session_id 两连发**,不再依赖解析(仍能演示"新会话出 session 事件 / 同会话第二轮无 session 事件")。
- demo_chat.sh 首版 `set -euo pipefail` + grep|head|sed 偶发非零退出,改 `set -uo pipefail` + 末尾 `exit 0`。

---

## ch01 收尾前状态(2026-09-08)

- 代码、测试、标注集、脚本、README、spec、plan、dev-notes 全部在仓库。剩余:code review 结论 + finish 段(下节追加)。
