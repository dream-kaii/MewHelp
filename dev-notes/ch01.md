# ch01 开发过程留痕

多章项目「电商智能客服系统」的 ch01 —— 纯对话跑通。按用户要求分阶段补记,每段记四样:**① 用户关键原话** / **② 我的关键产出** / **③ 用户拒绝或纠偏** / **④ 翻车与返工**。

---

## brainstorm 定稿(2026-09-08)

**① 用户原话(节选)**
> 我要用 Superpowers 模式做一个电商智能客服系统,第一步先跑通纯对话。……技术栈:Python + FastAPI + LangChain,模型接入直连上游,应用侧统一说 OpenAI 协议……本章不做:工具调用、Agent 循环……涉及具体库、框架、API 的用法,一律先用 Context7 MCP 查最新官方文档……Chat 页面是例外用 Vibe Coding 方式直接做,不套 brainstorm、TDD、code review。

> 现在你应该有 context7 了 然后 用writing-plans,编写我们的实现计划

**② 我的关键产出**
- 分类为 architectural(新项目)路径,按 brainstorm 流程先澄清后设计。
- 澄清结论两则:售后「诉求类型」= 枚举四类 {退货退款/仅退款/换货/维修},无法归类取 null,期望方案自由文本;模型接入统一 OpenAI 协议,Claude 经 OpenAI 兼容网关(不走 Anthropic 专用分支)。
- 设计草案(仓库布局、SSE 契约、token 预算启发式、with_structured_output 抽取、TDD/标注集分工、验收映射)在对话中分节给出并获准。
- `git init -b main` + `.gitignore`,仓库由空目录初始化为 git 仓库。
- spec 落盘:`docs/superpowers/specs/2026-09-08-ecommerce-cs-ch01-design.md`。

**③ 用户拒绝或纠偏**
- 我一度判断本会话无 Context7、准备用 WebFetch 顶替查文档;用户纠偏:「现在你应该有 context7 了」——工具随后确实出现在 MCP 列表,后续查文档走 Context7。
- 设计对话中 4 个待确认假设(① git init ② pyproject/pip ③ 服务端按 session_id 持历史 ④ 启发式 token 估算)用户未逐条驳回,默认按我的推荐带进计划,计划评审为最后否决点。

**④ 翻车与返工**
- 暂无实现性翻车。环境侧:会话早期工具列表确无 context7 MCP,与用户预期不符,属环境状态而非流程错误;已按用户提示复检确认可用。

---

## 计划评审通过(2026-09-08)

**① 用户原话**
> 现在你应该有 context7 了 然后 用writing-plans,编写我们的实现计划
> (AskUserQuestion 答复)ch01 实施计划评审意见?:通过;真模型 eval 和三连验收用哪家?:有真实 key

**② 我的关键产出**
- 计划落盘并提交:`docs/superpowers/plans/2026-09-08-ecommerce-cs-ch01-plan.md`(commit 2a97b44),10 任务。
- 动手前用 Context7 核实的 API 决策:SSE 改用 FastAPI 原生 `fastapi.sse.EventSourceResponse/ServerSentEvent`(spec 里"手写帧"的设想升级为官方组件);流式用 `model.astream`;`with_structured_output(method=...)` 做成 .env 可配;fake 模型走 `langchain_core.language_models.fake_chat_models.GenericFakeChatModel`。
- Self-review 三项通过;清掉了 Task 6 草稿里一处占位断言。
- 真模型走用户真实 key,.env 由用户填;provider 未指定,默认样例 DeepSeek 排头,Task 9 前再确认。

**③ 用户拒绝或纠偏**
- 无。4 个待确认假设(git init / pyproject / 服务端 session 语义 / token 启发式估算)随"通过"一并放行。

**④ 翻车与返工**
- writing-plans 初稿 Task 6 测试含一行占位断言(`store.get_session_ids() and None # placeholder`),违反 no-placeholder,提交前已用完整用例替换并在 Self-Review 记录——计划评审前修正,未造成返工。
