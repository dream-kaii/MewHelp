# ch03 开发过程留痕

多章项目「电商智能客服系统」的 ch03 —— 把 `query_faq` 从关键词查表升级为 **BGE-M3 + Milvus 的 dense 向量语义检索**,工具契约不变。按用户要求分阶段补记,每段记四样:**① 用户关键原话** / **② 我的关键产出** / **③ 用户拒绝或纠偏** / **④ 翻车与返工**。

---

## brainstorm 定稿(2026-09-11)

**① 用户原话(节选)**
> 我要用 Superpowers 模式给客服系统建知识库,把 query_faq 的内部实现从关键词查表升级成向量语义检索,工具的入参出参契约保持不变。……离线建库·文档处理……对话挖知识……双写落库……在线检索……
> 你帮我安装docker
> (答环境问题)Milvus 跑在哪:装 Docker Desktop(推荐);BGE-M3 用本地模型(推荐)
> (答口径问题)知识文档:我造样例文档(推荐);定时任务:就搞个命令能启动的job,我们可以自己选择部署或者直接执行;去重:哈希 + 向量近重(推荐)
> 通过

**② 我的关键产出**
- 环境排查结论:**本机无 Docker(CLI/Desktop 均无,ch02 已如此)、WSL Ubuntu 内也无 docker、无任何 Milvus/嵌入相关包** → 定死的 Milvus 默认路径跑不通,按用户规矩**停下问**;用户拍板装 Docker Desktop + BGE-M3 走本地权重。已用 winget 启动 Docker Desktop 安装。
- Context7 核对(动手前):`MilvusClient.create_collection(dimension, metric_type="COSINE", schema/index_params)`、`CollectionSchema/FieldSchema/DataType`、`insert/search/query/delete`;`BGEM3FlagModel('BAAI/bge-m3', use_fp16=True).encode(list, batch_size=12, max_length=...)['dense_vecs']`(1024 维)。
- 澄清/定稿结论:① 样例知识文档由我造(`knowledge_docs/`);② 定时任务 = **纯 CLI job**(`python -m scripts.mine_knowledge`),部署/调度由用户自选;③ 去重 = **归一化哈希精确 + BGE-M3 向量近重**;④ Milvus 集合**只存 id + 向量**;⑤ 新增 `knowledge_chunks` / `knowledge_staging` 两表。
- 设计文档落盘:`docs/superpowers/specs/2026-09-11-rag-knowledge-base-design.md`(分层、切分策略、双写幂等、job 形态、检索与阈值、测试分工、边界)。

**③ 用户拒绝或纠偏**
- 无纠偏;用户额外提出"你帮我安装docker"(见 ④)。

**④ 翻车与返工**
- **环境前提缺失(非返工)**:Milvus/BGE-M3 默认路径在本机不可用(Docker 缺席 + 无 torch),按约定停下询问并获用户授权安装,未自行改方案。
- 尚未产生代码返工(此阶段未写业务代码)。

---

## 环境准备:Docker + Milvus(2026-09-11)

**① 用户原话**
> 你帮我安装docker
> 已经启动起来了 我跳过登陆了

**② 我的关键产出**
- `winget install Docker.DockerDesktop` 装机成功(客户端 **29.7.2**,linux 容器);用户启动 Desktop 并跳过登录。
- `docker/milvus-compose.yml`:官方 standalone compose(**从 jsDelivr 取**,`raw.githubusercontent.com` 被网络策略挡);`DOCKER_VOLUME_DIRECTORY=./docker` 使卷落在 `docker/volumes/`(已加 .gitignore)。
- **版本对齐决策(重要)**:官方 master 的 compose 默认 `milvusdb/milvus:v3.0.0`,但 Context7 上 pymilvus 文档明确覆盖的是 **2.6.x 稳定 API 面** → 按"版本对不上是返工重灾区"的规矩,**钉到 Milvus v2.6.10 服务端 + pymilvus 2.6.x 客户端**(同代配套),而非跟随 master 的 v3.0.0。
- 三个容器全部 healthy:`milvus-standalone`(v2.6.10)/ `milvus-minio` / `milvus-etcd`;`9091/healthz → OK`,`19530` gRPC 可连。

**③ 用户拒绝或纠偏**
- 无(用户主动要求装 Docker 并已完成登录跳过)。

**④ 翻车与返工**
- **凭据助手缺失**:`docker compose pull` 报 `docker-credential-desktop not found` —— Docker Desktop 的 bin 目录不在 PATH;解决:命令前 `export PATH="/c/Program Files/Docker/Docker/resources/bin:$PATH"`。
- **Docker Hub 不可达**:`registry-1.docker.io` 连接超时导致镜像无法解析;解决:在 `~/.docker/daemon.json` 增 `registry-mirrors`(`docker.m.daocloud.io`、`docker.1ms.run`)并 `docker desktop restart`,随后拉取成功。
- **WebFetch 被策略拦截**(raw.githubusercontent.com)→ 改用 jsDelivr 镜像取官方 compose,未凭记忆手写。

---

## 计划评审通过(2026-09-11)

**① 用户原话**
> 通过

**② 我的关键产出**
- 计划落盘并提交:`docs/superpowers/plans/2026-09-11-rag-knowledge-base.md`(commit `1eda1db`,11 个 Task)。
- 写计划前用 Context7 复核:`MilvusClient.create_collection(name, dimension, metric_type="COSINE")` 快建自带索引、`search(data=[v], limit, output_fields)` 返回 distance、`CollectionSchema/FieldSchema/DataType`、`BGEM3FlagModel(...).encode(...)['dense_vecs']`(1024 维)。
- 计划自查修两处:接口漏声明 `ingest_markdown(..., skip_embed)`(测试要用);eval 脚本里一处 `any(... for _ in [0])` 别扭写法。
- 回归确认:ch02 全量 `pytest -q` → **83 passed**,新计划未破坏既有实现。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- 无。计划阶段未产生返工;两处自查问题在提交前修正。

---

## Task 1-3 完成:配置 / 知识表 / chunker(2026-09-11 · Subagent-Driven)

**① 用户原话**
> 通过(计划评审)

**② 我的关键产出**
- **Task 1** `fd80f40`:14 个 RAG 配置字段、`pymilvus>=2.6,<3` + `FlagEmbedding>=1.3` 依赖、`.env.example` ch03 段;评测通过(84 passed);Milvus 连通冒烟 `collections: []`。
- **Task 2** `de59bf9`:`sql/schema.sql` 追加 `knowledge_chunks` / `knowledge_staging`(幂等、纯追加),`models.py` 两个 ORM;init_db 列出 6 张表;86 passed。评审确认 DDL 与模型逐列一致、唯一键落在 `content_hash`/`dedupe_hash`。
- **Task 3** `c111357`:结构感知 `chunker.py`(标题层级/表格复制表头/递归切/重叠)+ 6 用例;92 passed。
- 执行模式:Subagent-Driven,台账在 `.superpowers/sdd/2026-09-11-rag-knowledge-base/`。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **Task 3 评审抓出 2 个 Important(计划代码缺陷)**:① 重叠规则**实测为零**(`_trim_to_sentence` 恒返回空串,`overlap` 形同虚设,且所给测试测不出);② 表格回退是"整节"级 —— 引导段+表格会触发定宽硬切,**整节丢表头、行结构塌掉**。裁决:重叠改为真实共享尾部句子片段;表格回退改为按行处理、超宽行子切分仍带表头。进入 fix round。
- **跨任务隐患(裁决并入 Task 6)**:`tests/db_utils.py::_TABLES` 未含两张新知识表 → 用例间不清表,UNIQUE `content_hash`/`dedupe_hash` 残留会让 Task 6/7 报难查的重复键错误。

---

## Task 4-7 完成:embedder / Milvus store / repository / 建库流水线(2026-09-11 · Subagent-Driven)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- **Task 4** `e3f1560`:`app/rag/embedder.py`(BGE-M3 懒加载 + `use_fp16=False` on CPU)与 `FakeEmbedder`;100 passed。
- **Task 5** `782cbef`:`app/rag/store.py`(Milvus `id + 1024 维 COSINE`);103 passed。实现者被迫做 3 处适配:**`count()` 改用 `query(count(*))`**(`get_collection_stats` 只算已 flush 段,滞后)、**`consistency_level="Strong"`**(默认 Bounded 导致 upsert 后读不到)、测试向量改为**方向不同**(否则 COSINE 恒 1.0,断言不成立)。
- **Task 6** `19eb1e2`:`app/db/repository_knowledge.py`(状态机/断点续跑/暂存区)+ 并行的两处捆绑修复;109 passed / 0 failed。`_TABLES` 修复被证实**承重**(不修则该测试因残留行 + UNIQUE 冲突必失败)。
- **Task 7** `b61b9e9`:`scripts/build_knowledge.py`(切分→双写→断点续跑)+ `upsert` 长度护栏 + 并发 ingest 竞态吸收;115 passed。CLI 冒烟:首次插入 2 → 重跑 0 → 空目录退出码 2。
- **环境裁决(BGE-M3 加载)**:本机 `huggingface.co` 不可达、`hf-mirror` 对快照内个别文件返 403 → repo-id 解析必失败。改用 `.env: EMBED_MODEL=<本地缓存快照绝对路径>`(类本就接受路径),实测经 `get_embedder(settings)` 加载成功 **dim=1024**(18.6s CPU),权重 2.27GB 已落盘。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **Task 5 API 适配**:计划的 `get_collection_stats`/默认一致性级别在本机 2.6.10 上语义不符(计数滞后、读后写不可见),由实现者查 Context7 后改法并记录;一致性级别保持 Strong 经我裁决(验收①要求"建完即可检索")。
- **Task 6 承重修复**:`_TABLES` 漏表导致 UNIQUE 残留 → 该测试必失败;按裁决在 Task 6 一并修掉(评审确认修复与诊断都正确)。
- **Task 7 评审 2 个 Important**:`--limit` 解析后被静默忽略;`except IntegrityError` 捕获过宽会无声丢块且归因错误 → 打回收窄 + 打通 limit + 补测试。
- **测试环境坑**:MySQL REPEATABLE READ 快照使"重跑后 pending 清零"断言看到旧快照,测试需显式 `commit()`(实现者已定位并记录)。

---

## Task 8 完成:挖知识 job(2026-09-11 · Subagent-Driven)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- `app/rag/miner.py`(LLM 抽 QA + 哈希/向量近重去重 + 暂存/提升)+ `scripts/mine_knowledge.py`(纯 CLI,`--limit/--lookback/--no-promote`);commit `1e90eed`,127 passed。
- 评审:**Needs fixes** —— Critical:空 `questions` 触发 `IndexError` 且 `mine` 无逐会话隔离 → **cron 每轮崩在同一会话、`promote_staged` 永不执行**,暂存区只涨不落库;Important:promote 缺 savepoint 容错、向量近重只覆盖"单会话内"(不满足 spec 的"整体去重")。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- 上述 3 项进入 fix round:无人值守韧性(空 questions 跳过 + 逐会话 try/except)、promote 逐行 savepoint、跨会话/对既有库的向量近重(mine 增加可选 store 参数)。
- 教训:计划把"job"写成纯 CLI 时,**没有把"无人值守失败模式"写进约束** —— 评审从 cron 视角补上了这一层。

---

## Task 9 完成:retriever + 替换 query_faq(2026-09-11 · Subagent-Driven)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- `app/rag/retriever.py`(`KnowledgeRetriever.search`:向量 → 阈值过滤 → 回 MySQL 拼答案;无命中含「未找到」)+ `app/tools/kb.py` 注入式改造 + `build_registry` 装配;commit `63f4af9`,147 passed。
- 真组件冒烟(真 BGE-M3 + Milvus + MySQL):**换说法的问句命中 0.724**、跑题被阈值挡回 `未找到` —— 验收①的语义检索已实证可用。
- 评审:**Needs fixes** —— ①每轮请求重建 embedder → **每轮重载 2.27GB(≈19s)** 且泄漏 Milvus 连接;②注入 retriever 后生产路径**硬依赖 Milvus,无降级**,集合未建时 FAQ 整体失败。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- 上述两项进入 fix round:embedder/store 改**进程级复用**;向量检索失败**降级回 SQL LIKE**(两路皆空才 `未找到`)。
- 教训:"把工具内部实现换掉"这类任务,计划只写了**功能替换**,没写**运行期资源生命周期与故障降级** —— 由评审查缺补上。

---

## Task 9-11 完成:retriever 收口 · 样例文档与评测 · 端到端验收(2026-09-11)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- **Task 9(两轮 fix)** `2ab917b`:`app/rag/runtime.py`(进程级 embedder/store 复用 + LazyStore 延迟连接)、retriever 向量失败才降级关键词、干净未命中直接回 `未找到`;154 passed。
- **Task 10** `2ff8441`:四份样例知识文档(含大表格与 1037 字长章节,确保递归切/表格切真被执行)、`eval_data/retrieval_samples.json`、`scripts/eval_retrieval.py`;真建库真评测 **4/4 = 100%**。
- **Task 11** `bce0e01`:`scripts/demo_rag.sh` + fixtures + README ch03 段;**验收① PASS**(`query_faq` ok,工具帧含 `相似度 0.724` 证明走向量召回,答复引用运费说明且无"查不到");**验收② PASS**(pending 5 → 重跑 → 0)。
- **最终全分支评审**:Needs fixes → 一次修复波 `867d60d`(156 passed)。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **Task 9 评审两轮**:第一轮指出"每轮重建 embedder → 每轮重载 2.27GB(≈19s)+ 连接泄漏";修复时**引入新违规**(低于阈值也回退关键词,违反"只跑 dense 单路"),复审核实实现者给的理由不成立(集合缺失本会抛异常),第二轮收窄到**仅服务类异常降级** —— 边界问题被两道评审夹住才定形。
- **最终评审抓出跨任务缺陷**:冷启动时同步 `encode`/`search` 阻塞事件循环且超过工具 8s 超时(首个 FAQ 查询冻住服务并先失败一次)→ 修复波改 `asyncio.to_thread`;另修 `--skip-embed` 静默吞 `--limit`(改为显式报错)。
- **验收②的诚实处理**:字面 `--skip-embed` 在已建好的库上是**空操作**(幂等),实现者改用"把 5 块重置为 pending"来诱导中断态,并主动披露 —— 该状态与真崩溃留下的状态完全一致,属等价演示,已记 Ruling。

## finish 与交付(2026-09-11)

**演示命令**
- 起 Milvus:`bash scripts/milvus.sh up|status|down`
- 建库:`python -m scripts.build_knowledge`(`--doc/--limit/--skip-embed`)
- 挖矿 job(可 cron/任务计划调用):`python -m scripts.mine_knowledge [--limit N --lookback D --no-promote]`
- 检索评测:`python -m scripts.eval_retrieval`(4/4)
- 验收:起服务后 `bash scripts/demo_rag.sh http://127.0.0.1:8000`

**测试结果**:`pytest -q` → **156 passed / 0 failed**;检索 eval **4/4**;验收① 相似度 0.724 命中、验收② pending 5→0。

**留痕**:本文件;spec `docs/superpowers/specs/2026-09-11-rag-knowledge-base-design.md`;plan `docs/superpowers/plans/2026-09-11-rag-knowledge-base.md`。

**已知限制(可接受)**:`doc_ok` 判定偏宽(前缀即可满足);改文档后旧 `content_hash` 块不会自动失效;`EMBED_MODEL` 绑定本机缓存路径;关键词检索仍作为**服务故障降级**保留(非并行第二路)。

---

## 追加:知识库录入页 /kb(Vibe Coding,2026-09-11)

**① 用户原话**
> 知识库录入页 http://localhost:8000/kb —— 材料清单与切块预览、手工录入、向量化补 pending、检索自测 样式风格跟首页一样就行

**② 我的关键产出**
- 后端适配路由 `app/routers/kb_admin.py`:`GET /api/kb/docs`(材料清单+状态计数)、`GET /api/kb/chunks`(切块预览)、`POST /api/kb/chunks`(手工录入,落 pending)、`POST /api/kb/embed_pending`(补向量化,幂等)、`GET /api/kb/search`(拼装文本 + 命中明细含相似度);服务经 `app.state.kb_services` 注入。
- repository 增两个只读查询 `doc_summary` / `list_chunks`(维持"唯一写 SQL 处"约定)。
- 页面 `web/kb.html`(沿用首页暖色 token)+ `GET /kb` 路由;首页头部加「📚 知识库」入口。
- 契约测试 `tests/test_kb_admin_routes.py`(7 例,注入 `ConstantEmbedder` + 测试用 Milvus 集合 + `NullPool` 测试引擎,不打真网络)。
- 实测:全量 **163 passed**;线上冒烟 `/kb` 200、清单 27 块全 embedded、检索自测「邮费是多少」命中 `运费说明.md`(0.6749/0.6733/0.5761)。

**③ 用户拒绝或纠偏**
- 用户先问"前端是不是还没有" → 澄清:聊天页 ch01/ch02 已有(ch03 未含前端任务);随后提出本页需求。

**④ 翻车与返工**
- 无。按约定本页走 Vibe(不套 brainstorm/TDD/review);但**新增的后端接口仍补了契约测试**,以免录入页改动破坏既有链路。
