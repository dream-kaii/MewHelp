# ch02 开发过程留痕

多章项目「电商智能客服系统」的 ch02 —— 用 Function Calling 给客服聊天接上"查数据"能力(FastAPI + SQLAlchemy + MySQL + LangChain @tool)。按用户要求分阶段补记,每段记四样:**① 用户关键原话** / **② 我的关键产出** / **③ 用户拒绝或纠偏** / **④ 翻车与返工**。

---

## brainstorm 定稿(2026-09-10)

**① 用户原话(节选)**
> 我要用 Superpowers 模式给现有的客服系统装上查数据的能力,用 Function Calling 让模型自己决定调什么工具——工具链要长在客服聊天本身,用户在聊天页问一句就能触发。
> ……单轮调用:模型调一次工具就收敛。
> (答 MySQL 环境问题)用本地的MySQL数据库 下面是建表语句 …(粘贴 conversations/messages/faq/tickets 完整 DDL)
> 已填完继续,你来建表建库
> 修改一下:单轮**工具调用(模型调一次工具就收敛)
> 通过

**② 我的关键产出**
- 环境排查结论:**本机 Docker 不可用**(无 docker CLI/服务/Desktop 主程序,只剩 `ProgramData\DockerDesktop` 安装日志残留;WSL Ubuntu 24.04 内也无 docker/mysql),但发现 **3306 本地 MySQL 正在监听** → 按用户指示改用本机 MySQL。
- 建库建表完成:`sql/schema.sql`(用户 DDL + `IF NOT EXISTS`)、`scripts/init_db.py`,`mewhelp` 库四表齐(conversations/messages/faq/tickets,均 0 行)。
- 澄清/定稿结论:① 多轮历史**以数据库为准**,ch01 内存 SessionStore 退役;② 工具 mock 数据**确定性**(hash 播种);③ `user_id` 由前端 localStorage 生成持久 id;④ 单轮里多个 tool_calls **全执行**(仍属同一轮)。
- 设计文档落盘:`docs/superpowers/specs/2026-09-10-function-calling-tools-design.md`(含"唯一一次工具往返 + 收敛阶段不绑工具"的硬约束改法)。
- 文档核对(Context7):LangChain 1.x `langchain.tools.tool`/`bind_tools`/`AIMessage.tool_calls`/`ToolMessage(content, tool_call_id=)`;SQLAlchemy 2.0 async `create_async_engine`+`async_sessionmaker`+`DeclarativeBase`,MySQL 异步方言。

**③ 用户拒绝或纠偏**
- **技术选型纠偏**:定死的"Docker 起 MySQL"在本机不可用,我按规矩停下问 → 用户改为**用本机已有 MySQL** 并直接给出 DDL(未自行换方案,符合约定)。
- **语义纠偏**:我初版写"第一轮 ainvoke 收集工具 + 第二轮 astream 收敛",用户要求明确为"**模型调一次工具就收敛**" → 改为"唯一一次工具往返 + 收敛阶段不绑工具",从机制上杜绝二次调用。

**④ 翻车与返工**
- 环境侧意外:Docker 缺失导致本章基建方式临时改道(用户拍板,非返工)。
- 无代码返工(此阶段未写业务代码;建表脚本一次跑通)。

---

## 计划评审通过(2026-09-10)

**① 用户原话**
> (承接上一条)通过
> 通过,没问题就帮我启动前后端我进行测试

**② 我的关键产出**
- 计划落盘并提交:`docs/superpowers/plans/2026-09-10-function-calling-tools.md`(12 任务)。
- 计划期完成的核对:`aiomysql 0.3.2` / `sqlalchemy 2.0.52` 安装并实测连通本机 **MySQL 5.7.20**(四表在位);Context7 核实 `@tool`/`bind_tools`/`AIMessageChunk.tool_call_chunks`/`ToolMessage` 与 SQLAlchemy 2.0 async API。
- 自查修正三处会跑不通的写法:Task 9 占位式 `build_budget_guard` 与 `svc._registry` hack → 改 `registry_factory(cid)` 注入;测试库统一 `mewhelp_test`(避免路由测试污染开发库);`client` 同步 fixture 不依赖异步 fixture。
- 执行模式:Subagent-Driven(用户既定偏好)。

**③ 用户拒绝或纠偏**
- 无(计划一次通过)。用户同时要求"启动前后端测试"——已说明服务需待实现完成后再起。

**④ 翻车与返工**
- 计划初稿的上述三处缺陷在提交前自查修掉,未造成执行期返工。

---

## Task 1 完成:配置与 DB 基建(2026-09-10 · Subagent-Driven)

**① 用户原话**
> (无新输入;执行阶段由控制方按计划派子代理)

**② 我的关键产出**
- 子代理实现:`app/config.py` 增 9 个字段(MYSQL_*/TOOL_*/MOCK_SEED_SALT)、`.env.example` 同步、`app/db/base.py`(Base/build_database_url/get_engine/get_sessionmaker/dispose_engine/create_all);`tests/test_db_base.py` 4 测;commit `6b7e0ee`。
- 任务评审(独立子代理):1 Important + 4 Minor;Important = pyproject 未声明 sqlalchemy/aiomysql/pymysql(仅存在于 .venv,新环境会 import 失败)。
- fix round 1:打回原实现者补依赖声明;Minor 全部记入 ledger 留待最终评审。
- Context7 核对记录在实现者报告(report 文件),非本 notes。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **计划 Task 1 测试断言自相矛盾**:`url.endswith("/db")` 与同时要求 URL 含 `charset=utf8mb4` 查询串不可兼得(实现者诊断正确,改为 `split("?")[0].endswith(...)`)。已作为 Ruling 记入 ledger。
- **漏声明依赖**:计划文件清单里没有 pyproject,导致实现只装不声明;由任务评审抓出并修复。属计划覆盖漏洞,非实现质量事故。

---

## Task 2 完成:SQLAlchemy 模型(2026-09-10 · Subagent-Driven)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- 子代理实现四个 ORM 模型镜像 `sql/schema.sql`(conversations/messages/faq/tickets);`tests/test_db_models.py` 4 测;commit `df13c4d`,全量 46 passed。
- 任务评审(独立子代理)逐列对照 DDL:**Spec ✅ 全部一致**(表名/列/可空/枚举集/主键,含 tickets 以 ticket_no 为主键);仅 4 个 Minor(枚举集覆盖缺口、BigInteger 有符号、索引名差异、未具名 warning),均记入 ledger 留待最终评审。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- 无返工。Minor 多为计划测试覆盖缺口(计划只给了 4 个用例),非实现缺陷。

---

## Task 3 完成:测试库 fixtures(2026-09-10 · Subagent-Driven)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- 子代理实现 `tests/db_utils.py`(建库/按 schema.sql 建表/清表,唯一裸 SQL 处)、重写 `tests/conftest.py`(环境变量覆盖到 `mewhelp_test`、`client`/`session_factory`/`db_session` fixtures)、`tests/test_db_fixture.py`;commit `c38b005`,全量 48 passed。
- 任务评审:**Spec ✅,Task quality Approved**。关键澄清:TRUNCATE 只指向 `mysql_test_database`,实现者报告里"会清空开发库"的说法不成立(评审逐行核了决定 DB 名的控制流)。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- 无返工。1 个 Minor 值得留意:`setdefault` 挡不住外部导出的 `MYSQL_DATABASE`(默认引擎可能连开发库 = 污染风险),已记 ledger 交最终评审 triage。

---

## Task 4 完成:repository(2026-09-10 · Subagent-Driven)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- 子代理实现 `app/db/repository.py`(会话/消息/FAQ LIKE/工单号生成,7 个 async 函数)+ `tests/test_repository.py`;commit `41322c7`,全量 52 passed。
- 任务评审:Spec ✅ + Task quality Approved,但报 2 个 Important:① `create_ticket` 重试用整事务 `rollback()`;② 冲突/重试分支零测试覆盖。
- fix round 1:裁决改用 **SAVEPOINT**(`begin_nested`)只回滚失败插入,并补 `next_ticket_no` 直测与冲突重试测。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **实现偏差(已裁决接受)**:计划代码 flush 后同步读 `conv.status` 触发 `MissingGreenlet`(server_default 未被加载、MySQL 无 RETURNING)→ 实现者在创建路径加 `refresh`。属计划代码未考虑 server_default,已记 Ruling。
- **计划缺陷**:`create_ticket` 整事务回滚语义 + 重试分支无测试,均由任务评审抓出,进入 fix round。

---

## Task 5 完成:mock 数据 + 三个业务工具(2026-09-10 · Subagent-Driven)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- 子代理实现 `app/tools/mockdata.py`(hash 播种、确定性)与 `app/tools/business.py`(query_order/query_product/query_logistics 三个 `@tool`,返回 JSON 字符串);commit `b45d58b`,全量 59 passed。
- 任务评审:**Spec ✅ / Approved**。确认确定性真实(同输入同输出、异输入异输出)、salt 确实参与种子、三个工具的 docstring/参数与 brief 逐字一致(模型选型面)。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- 无返工。3 个 Minor 全为计划指定:`mock_order.product_id` 与所选商品无关联;盐参与种子无回归测试;其余两工具的 schema/invoke 未覆盖。已记 ledger。

---

## Task 6 / Task 7 完成:FAQ·工单工具 与 工具注册表(2026-09-10 · Subagent-Driven)

**① 用户原话**
> (无新输入)

**② 我的关键产出**
- **Task 6**:`app/tools/kb.py`(query_faq,查不到返回含「未找到」的明确提示)、`app/tools/ops.py`(create_ticket,非法类型返回提示不抛);commit `5da109b`,62 passed。评审 Spec ✅ Approved(仅计划内的覆盖缺口)。
- **Task 7**:`app/tools/registry.py`(注册/派发/参数校验/超时重试)+ 测试;commit `2a07092`,67 passed。评审抓出 1 Important(超时取消不了线程中的同步工具 → 写工具会被重试两次)→ fix round 1 增 **`non_retryable` 构建期黑名单**(`b9aee9f`,68 passed)。
- 传递项:Task 10 构建注册表时须登记 `non_retryable={"create_ticket"}`(已记 ledger)。

**③ 用户拒绝或纠偏**
- 无。

**④ 翻车与返工**
- **计划缺陷(评审抓出)**:`asyncio.wait_for` 无法取消线程中运行的同步工具,重试会让 `create_ticket` 写两遍;裁决用构建期黑名单解决,未让副作用工具重试。
- **计划测试写法不可行**:brief 里 `reg.by_name("echo").ainvoke = slow` 在 pydantic v2 的 StructuredTool 上会抛 `ValueError`;实现改用本地真慢工具,强度更高(评审确认属改进而非绕过)。
- **环境故障**:分类器再次不可用 → Task 7 的 scoped re-review 无法派子代理,改由控制方只读逐行核验 5 项修复并记 Ruling;期间写文件亦被拦,故本段留痕延后补记。

---

## Task 8 / 9 / 10 完成:单轮往返 · 两段式 ChatService · 路由契约(2026-09-10 · Subagent-Driven)

**① 用户原话**
> 能不能别老想我授权 给你一次性授权完行不行

**② 我的关键产出**
- **Task 8** `app/agent/tool_runner.py`(收集 + 并行执行)+ 7 测;commit `f7a465d`,75 passed。评审确认 **跨 chunk 合并 tool_calls 在本版 langchain-core 1.6.2 确实成立**(brief 只喂单 chunk 测不出),实现者补了跨 chunk 用例。
- **Task 9** `ChatService` 两段式改造 + `context.to_langchain_messages`;commit `c1e5c42`。评审确认三条硬约束成立(唯一一次工具往返、收敛段不绑工具、DB 为准 + 帧契约)。6 个 ch01 旧契约测试失败按计划归 Task 10。
- **Task 10** 路由新契约(conversation_id/user_id)、删除 `app/sessions.py` 与其测试、重写 ch01 路由/服务测试 + 新增工具路由测试;commit `f732c19` → fix `622c837`,**76 passed**。
- 权限:按用户要求一次性扩了 `.claude/settings.local.json` 的 allowlist(venv python 全量、bash scripts、git、curl、ls/grep/cat/sed/awk/node/sleep 等),命中即不过分类器。

**③ 用户拒绝或纠偏**
- 用户明确要求"一次性授权完",不要再频繁触发授权 → 落为 allowlist 扩容;并向用户说明:非 Bash 工具(Write/Edit/派子代理)在 auto 模式仍受分类器管辖,彻底免打扰需切 accept-edits 模式。

**④ 翻车与返工**
- **Task 10 评审抓出 Important(计划缺陷)**:会话标识从 uuid 变自增整数后,`get_or_create_conversation` 不校验 `user_id` → 可枚举读/写他人会话。裁决按 `(id, user_id)` 归属过滤,查不到即新建;fix round 1 修复并补 repository/路由两级测试。
- **Task 10 实现者额外发现真实 bug**:池化 aiomysql 连接绑定事件循环,`TestClient` 每例新循环会炸 → 在 `app/main.py` lifespan 关闭时 `dispose_engine()`(同时也是正确的生产停机清理)。
- Task 9 两处计划偏差经评审确认为合理修正:测试替身的 `bind_tools` 改为非变更;注册表为空时不绑工具(保持 ch01 纯聊天流式)。
